<ui:MainView
	xmlns="https://github.com/avaloniaui"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:ui="using:EstragoniaTemplate.UI"
	xmlns:dataTemplates="using:EstragoniaTemplate.UI.DataTemplates"
	xmlns:vm="using:EstragoniaTemplate.UI.ViewModels"
	xmlns:controls="using:EstragoniaTemplate.UI.Controls"
	x:Class="EstragoniaTemplate.UI.Views.MainView"
	x:DataType="vm:MainViewModel"
	RenderOptions.TextRenderingMode="Antialias">
	
	<ui:MainView.DataTemplates>
		<ui:ViewLocator/>
	</ui:MainView.DataTemplates>

	<TransitioningContentControl Content="{Binding CurrentViewModel}">
		<TransitioningContentControl.PageTransition>
			<CompositePageTransition>
				<controls:TransitionDisableFrom Duration="0.3" />
				<controls:SequentialFade Duration="0.3" />
			</CompositePageTransition>
		</TransitioningContentControl.PageTransition>
	</TransitioningContentControl>

</ui:MainView>